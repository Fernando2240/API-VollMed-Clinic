spring.application.name=api
spring.datasource.url=jdbc:postgresql://localhost/vollmed_api
spring.datasource.username=postgres
spring.datasource.password=root
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.error.include-stacktrace=never

api.security.secret=${JWT_SECRET:123456} //Propiedad del la misma api, se define una variable que puede ser de entorno pero tambien tiene un valor por defecto